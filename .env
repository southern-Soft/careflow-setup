# =============================================================================
# CENTRAL CONFIGURATION FILE
# =============================================================================
# Change values here and they will update throughout the entire system
# This file is the single source of truth for all environment configuration
#
# NOTE: After changing this file:
# 1. Restart Docker services: docker-compose down && docker-compose up -d
# 2. For frontend/backend only changes without Docker, restart the service
# =============================================================================

# --- Port Configurations ---
# External ports exposed on your host machine

# Databases (Host side)
USERS_DB_PORT=5435
ORDERS_DB_PORT=5436
USERS_IMPLEMENTATION_DB_PORT=5437
END_DEVICE_DB_PORT=5438
GATEWAY_DB_PORT=5439
CLIENTS_DB_PORT=5440

# Applications (Host side)
BACKEND_PORT=1679
FRONTEND_SALES_PORT=1677
FRONTEND_IMPLEMENTATION_PORT=1678

# --- Database Configuration ---
# PostgreSQL connection settings

POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_PORT=5432

# Database host names (Docker internal)
POSTGRES_HOST_USERS=db-users
POSTGRES_HOST_ORDERS=db-orders
POSTGRES_HOST_USERS_IMPLEMENTATION=IOT_users_implementation
POSTGRES_HOST_END_DEVICE=IOT_end_device
POSTGRES_HOST_GATEWAY=IOT_gateway
POSTGRES_HOST_CLIENTS=IOT_clients



# Database names
POSTGRES_DB_USERS=IOT_users
POSTGRES_DB_ORDERS=IOT_orders
POSTGRES_DB_USERS_IMPLEMENTATION=IOT_users_implementation
POSTGRES_DB_END_DEVICE=IOT_end_device
POSTGRES_DB_GATEWAY=IOT_gateway
POSTGRES_DB_CLIENTS=IOT_clients


# --- Security & Authentication ---
# JWT and authentication settings

SECRET_KEY=YOUR-SECRET-KEY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# --- CORS Configuration ---
# Control which origins can access the API

CORS_ORIGINS=*

# --- Application Configuration ---
# General application settings

ENVIRONMENT=production
PROJECT_NAME=Southern IOT System
API_VERSION=1.0.0

# --- Frontend Configuration ---
# Next.js specific settings

PROTOCOL=http
HOST_NAME=localhost
NEXT_PUBLIC_USER_COOKIE=sa-iot-user

# API URLs (Docker internal - using IP address to avoid DNS issues)
API_URL=http://localhost:1679
BACKEND_URL=http://localhost:1679

# --- Production Overrides ---
# Uncomment and modify for production deployment

# PRODUCTION_HOST_NAME=localhost
# PRODUCTION_FRONTEND_PORT=1678
# NEXT_PUBLIC_ENCRYPT_SECRETS=false

# =============================================================================
# Connectivity Notes:
# - Inside Docker: Services communicate via internal names (e.g., http://backend:1679)
# - From your browser: Use http://localhost:1679/docs for Backend API
# - From your browser: Use http://localhost:1678 for Frontend
# =============================================================================
