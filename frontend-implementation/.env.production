# Production Environment Configuration (CURRENT LIVE SERVER)

PROTOCOL=http
HOST_NAME=localhost:1678
PORT=1678

# Site URL - what users access
NEXT_PUBLIC_SITE_URL=http://localhost:1678

# Encryption Settings (keep or disable for now)
NEXT_PUBLIC_ENCRYPT_SECRETS=false
# NEXT_PUBLIC_CRYPTOGRAPH_IV=...
# NEXT_PUBLIC_CRYPTOGRAPH_KEY=...

# API Masking - Route through Next.js rewrite
NEXT_PUBLIC_API_MASK_URL=/external-api

# Production Backend - internal Docker network (using IP to avoid DNS issues)
API_URL=http://localhost:1679
BACKEND_URL=http://localhost:1679

# Auth Cookie Name
NEXT_PUBLIC_USER_COOKIE=sa-iot-user