# Base Environment Configuration
# This file references the central .env at the project root
# For port and configuration changes, edit ../​.env instead

PROTOCOL=http
HOST_NAME=localhost
PORT=1678

# Site URL (composed from above values)
NEXT_PUBLIC_SITE_URL=${PROTOCOL}://${HOST_NAME}:${PORT}

# API Masking - Route through Next.js rewrite
NEXT_PUBLIC_API_MASK_URL=/external-api

# Backend API Configuration (Docker internal - using IP to avoid DNS issues)
API_URL=http://localhost:1679
BACKEND_URL=http://localhost:1679
